---
import AdminPage from '../../layouts/AdminPage.astro';
import AdminLayout from '../../components/admin/AdminLayout';

// Mock data for dashboard
const stats = [
  { label: 'New Leads', value: 12, change: '+3', trend: 'up' },
  { label: 'Contacted', value: 8, change: '+2', trend: 'up' },
  { label: 'Scheduled', value: 5, change: '+1', trend: 'up' },
  { label: 'Response Rate', value: '94%', change: '+2%', trend: 'up' },
];

const recentLeads = [
  { id: '1', name: 'Sarah Chen', email: 'sarah@acme.com', company: 'Acme Inc', status: 'new', message: 'Interested in your AI team services', createdAt: '2024-01-15T10:30:00Z' },
  { id: '2', name: 'Mike Ross', email: 'mike@startup.io', company: 'Startup.io', status: 'contacted', message: 'Looking for web development help', createdAt: '2024-01-15T09:15:00Z' },
  { id: '3', name: 'Emma Wilson', email: 'emma@tech.co', company: 'Tech Co', status: 'scheduled', message: 'Booked a call for tomorrow', createdAt: '2024-01-14T16:45:00Z' },
  { id: '4', name: 'James Lee', email: 'james@design.studio', company: 'Design Studio', status: 'customer_waiting', message: 'Can you provide more details?', createdAt: '2024-01-14T14:20:00Z' },
  { id: '5', name: 'Anna Park', email: 'anna@consulting.com', company: 'Consulting LLC', status: 'escalated', message: 'Urgent: Need immediate response', createdAt: '2024-01-13T11:00:00Z' },
];

const statusColors: Record<string, string> = {
  new: 'bg-blue-100 text-blue-800',
  contacted: 'bg-yellow-100 text-yellow-800',
  customer_waiting: 'bg-orange-100 text-orange-800',
  scheduled: 'bg-green-100 text-green-800',
  closed_won: 'bg-green-100 text-green-800',
  closed_lost: 'bg-gray-100 text-gray-600',
  escalated: 'bg-red-100 text-red-800',
  opted_out: 'bg-gray-100 text-gray-400',
};

const statusLabels: Record<string, string> = {
  new: 'New',
  contacted: 'Contacted',
  customer_waiting: 'Customer Waiting',
  scheduled: 'Scheduled',
  closed_won: 'Closed Won',
  closed_lost: 'Closed Lost',
  escalated: 'Escalated',
  opted_out: 'Opted Out',
};
---

<AdminPage title="Dashboard" currentPath="/admin">
  <AdminLayout currentPath="/admin">
    <div class="p-8">
      <!-- Header -->
      <div class="mb-8">
        <h1 class="text-3xl font-bold font-heading text-charcoal mb-2">Dashboard</h1>
        <p class="text-gray-500">Overview of your lead pipeline</p>
      </div>

      <!-- Stats Grid -->
      <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-4 gap-6 mb-8">
        {stats.map((stat) => (
          <div class="bg-white rounded-xl p-6 shadow-sm border border-gray-100">
            <p class="text-sm text-gray-500 mb-1">{stat.label}</p>
            <div class="flex items-end gap-2">
              <span class="text-3xl font-bold text-charcoal">{stat.value}</span>
              <span class={`text-sm font-medium mb-1 ${stat.trend === 'up' ? 'text-green-600' : 'text-red-600'}`}>
                {stat.change}
              </span>
            </div>
          </div>
        ))}
      </div>

      <!-- Recent Leads -->
      <div class="bg-white rounded-xl shadow-sm border border-gray-100">
        <div class="p-6 border-b border-gray-100 flex items-center justify-between">
          <h2 class="text-lg font-semibold font-heading text-charcoal">Recent Leads</h2>
          <a href="/admin/leads" class="text-sm text-pounce-orange hover:text-pounce-orange-dark font-medium">
            View all →
          </a>
        </div>
        <div class="divide-y divide-gray-100">
          {recentLeads.map((lead) => (
            <div class="p-6 hover:bg-gray-50 transition-colors">
              <div class="flex items-start justify-between">
                <div class="flex-1">
                  <div class="flex items-center gap-3 mb-1">
                    <h3 class="font-semibold text-charcoal">{lead.name}</h3>
                    <span class={`inline-flex items-center px-2.5 py-0.5 rounded-full text-xs font-medium ${statusColors[lead.status] || 'bg-gray-100 text-gray-800'}`}>
                      {statusLabels[lead.status] || lead.status}
                    </span>
                  </div>
                  <p class="text-sm text-gray-500">{lead.email} · {lead.company}</p>
                  <p class="text-sm text-gray-600 mt-2">{lead.message}</p>
                </div>
                <span class="text-xs text-gray-400">{new Date(lead.createdAt).toLocaleDateString()}</span>
              </div>
            </div>
          ))}
        </div>
      </div>
    </div>
  </AdminLayout>
</AdminPage>
