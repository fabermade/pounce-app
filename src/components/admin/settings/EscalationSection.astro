---
// Escalation Rules section with add/remove triggers
// Props: triggers array, notifyEmail

interface Props {
  triggers?: string[];
  notifyEmail?: string;
}

const { triggers = [], notifyEmail = '' } = Astro.props;
---

<div id="section-escalation" class="bg-white rounded-2xl shadow-sm border border-gray-200 p-6 transition-all">
  <h2 class="text-lg font-bold font-heading text-charcoal mb-6">Escalation Rules</h2>
  <div class="space-y-4">
    <div class="space-y-3">
      <div class="flex items-center justify-between">
        <label class="text-xs font-bold text-gray-500 uppercase tracking-wider">Trigger Phrases</label>
        <button type="button" data-add-trigger class="px-3 py-1.5 bg-pounce-orange text-white rounded-lg text-xs font-bold hover:bg-pounce-orange-dark transition-all shadow-sm">
          + Add Trigger
        </button>
      </div>
      <p class="text-xs text-gray-400 italic">If a lead says any of these, the AI will escalate to a human.</p>
      <div id="triggers-list" class="flex flex-wrap gap-2">
        {/* Hidden input ensures empty array is sent when all triggers are removed */}
        <input type="hidden" name="escalation.triggerPhrases" value="__empty_array__" data-empty-marker />
        {triggers.length === 0 && (
          <span class="text-xs text-gray-400 italic" data-empty-text>No triggers configured.</span>
        )}
        {triggers.map((phrase, i) => (
          <span class="trigger-tag inline-flex items-center gap-1 px-2.5 py-1 bg-red-50 text-red-700 rounded-lg text-xs font-bold border border-red-100 transition-all hover:bg-red-100" data-index={i}>
            <input
              type="text"
              name={`escalation.triggerPhrases[${i}]`}
              value={phrase}
              class="bg-transparent border-none focus:outline-none text-xs w-24 placeholder:text-red-300"
            />
            <button type="button" data-remove-trigger class="text-red-400 hover:text-red-600 transition-colors">
              <svg class="w-3 h-3" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M6 18L18 6M6 6l12 12" />
              </svg>
            </button>
          </span>
        ))}
      </div>
    </div>
    <div class="space-y-1.5">
      <label class="text-xs font-bold text-gray-500 uppercase tracking-wider">Notify Email</label>
      <input
        type="email"
        name="escalation.notifyEmail"
        value={notifyEmail}
        placeholder="you@yourcompany.com"
        class="w-full px-3 py-2 rounded-lg border border-gray-200 focus:outline-none focus:ring-2 focus:ring-pounce-orange/20 focus:border-pounce-orange text-sm transition-all placeholder:text-gray-300"
      />
      <p class="text-xs text-gray-400 italic">You'll be notified when a lead needs human attention.</p>
    </div>
  </div>
</div>

<script>
  const triggersList = document.getElementById('triggers-list');

  document.querySelector('[data-add-trigger]')?.addEventListener('click', () => {
    const count = triggersList?.querySelectorAll('.trigger-tag').length || 0;
    const span = document.createElement('span');
    span.className = 'trigger-tag inline-flex items-center gap-1 px-2.5 py-1 bg-red-50 text-red-700 rounded-lg text-xs font-bold border border-red-100 transition-all hover:bg-red-100';
    span.dataset.index = String(count);
    span.innerHTML = `
      <input type="text" name="escalation.triggerPhrases[${count}]" placeholder="Phrase" class="bg-transparent border-none focus:outline-none text-xs w-24 placeholder:text-red-300" />
      <button type="button" data-remove-trigger class="text-red-400 hover:text-red-600 transition-colors">
        <svg class="w-3 h-3" fill="none" viewBox="0 0 24 24" stroke="currentColor">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M6 18L18 6M6 6l12 12" />
        </svg>
      </button>
    `;
    triggersList?.appendChild(span);
    const emptyText = triggersList?.querySelector('[data-empty-text]');
    if (emptyText) emptyText.remove();
  });

  triggersList?.addEventListener('click', (e) => {
    const btn = (e.target as HTMLElement).closest('[data-remove-trigger]');
    if (btn) {
      btn.closest('.trigger-tag')?.remove();
      const emptyText = triggersList.querySelector('[data-empty-text]');
      if (triggersList.querySelectorAll('.trigger-tag').length === 0 && !emptyText) {
        const span = document.createElement('span');
        span.className = 'text-xs text-gray-400 italic';
        span.dataset.emptyText = '';
        span.textContent = 'No triggers configured.';
        triggersList.appendChild(span);
      }
    }
  });
</script>
