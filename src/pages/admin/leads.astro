---
import AdminPage from '../../layouts/AdminPage.astro';
import AdminLayout from '../../components/admin/AdminLayout';

// Mock leads data
const leads = [
  { id: '1', name: 'Sarah Chen', email: 'sarah@acme.com', company: 'Acme Inc', status: 'new', message: 'Interested in your AI team services', createdAt: '2024-01-15T10:30:00Z' },
  { id: '2', name: 'Mike Ross', email: 'mike@startup.io', company: 'Startup.io', status: 'contacted', message: 'Looking for web development help', createdAt: '2024-01-15T09:15:00Z' },
  { id: '3', name: 'Emma Wilson', email: 'emma@tech.co', company: 'Tech Co', status: 'scheduled', message: 'Booked a call for tomorrow', createdAt: '2024-01-14T16:45:00Z' },
  { id: '4', name: 'James Lee', email: 'james@design.studio', company: 'Design Studio', status: 'customer_waiting', message: 'Can you provide more details?', createdAt: '2024-01-14T14:20:00Z' },
  { id: '5', name: 'Anna Park', email: 'anna@consulting.com', company: 'Consulting LLC', status: 'escalated', message: 'Urgent: Need immediate response', createdAt: '2024-01-13T11:00:00Z' },
  { id: '6', name: 'David Kim', email: 'david@enterprise.com', company: 'Enterprise Corp', status: 'closed_won', message: 'Signed contract for AI team', createdAt: '2024-01-10T08:30:00Z' },
  { id: '7', name: 'Lisa Wang', email: 'lisa@smallbiz.com', company: 'Small Biz', status: 'closed_lost', message: 'Budget constraints, maybe next quarter', createdAt: '2024-01-08T13:15:00Z' },
  { id: '8', name: 'Tom Brown', email: 'tom@agency.com', company: 'Creative Agency', status: 'opted_out', message: 'Unsubscribed from emails', createdAt: '2024-01-05T16:00:00Z' },
];

const statusFilters = [
  { value: 'all', label: 'All Leads' },
  { value: 'new', label: 'New' },
  { value: 'contacted', label: 'Contacted' },
  { value: 'customer_waiting', label: 'Customer Waiting' },
  { value: 'scheduled', label: 'Scheduled' },
  { value: 'closed_won', label: 'Closed Won' },
  { value: 'closed_lost', label: 'Closed Lost' },
  { value: 'escalated', label: 'Escalated' },
];

const statusColors: Record<string, string> = {
  new: 'bg-blue-100 text-blue-800',
  contacted: 'bg-yellow-100 text-yellow-800',
  customer_waiting: 'bg-orange-100 text-orange-800',
  scheduled: 'bg-green-100 text-green-800',
  closed_won: 'bg-green-100 text-green-800',
  closed_lost: 'bg-gray-100 text-gray-600',
  escalated: 'bg-red-100 text-red-800',
  opted_out: 'bg-gray-100 text-gray-400',
};

const statusLabels: Record<string, string> = {
  new: 'New',
  contacted: 'Contacted',
  customer_waiting: 'Customer Waiting',
  scheduled: 'Scheduled',
  closed_won: 'Closed Won',
  closed_lost: 'Closed Lost',
  escalated: 'Escalated',
  opted_out: 'Opted Out',
};
---

<AdminPage title="Leads" currentPath="/admin/leads">
  <AdminLayout currentPath="/admin/leads">
    <div class="p-8">
      <!-- Header -->
      <div class="mb-8">
        <h1 class="text-3xl font-bold font-heading text-charcoal mb-2">Leads</h1>
        <p class="text-gray-500">Manage and track your lead pipeline</p>
      </div>

      <!-- Filters -->
      <div class="flex flex-wrap gap-2 mb-6">
        {statusFilters.map((filter) => (
          <button
            class={`px-4 py-2 rounded-lg text-sm font-medium transition-colors ${
              filter.value === 'all'
                ? 'bg-charcoal text-white'
                : 'bg-white text-gray-600 hover:bg-gray-50 border border-gray-200'
            }`}
          >
            {filter.label}
          </button>
        ))}
      </div>

      <!-- BOLT: Need GET /api/admin/leads with filters -->
      
      <!-- Leads Table -->
      <div class="bg-white rounded-xl shadow-sm border border-gray-100 overflow-hidden">
        <div class="overflow-x-auto">
          <table class="w-full">
            <thead>
              <tr class="border-b border-gray-100">
                <th class="text-left py-4 px-6 text-xs font-medium text-gray-500 uppercase tracking-wider">Lead</th>
                <th class="text-left py-4 px-6 text-xs font-medium text-gray-500 uppercase tracking-wider">Status</th>
                <th class="text-left py-4 px-6 text-xs font-medium text-gray-500 uppercase tracking-wider">Message</th>
                <th class="text-left py-4 px-6 text-xs font-medium text-gray-500 uppercase tracking-wider">Date</th>
                <th class="text-right py-4 px-6 text-xs font-medium text-gray-500 uppercase tracking-wider">Actions</th>
              </tr>
            </thead>
            <tbody class="divide-y divide-gray-100">
              {leads.map((lead) => (
                <tr class="hover:bg-gray-50 transition-colors">
                  <td class="py-4 px-6">
                    <div class="flex items-center gap-3">
                      <div class="w-10 h-10 rounded-full bg-gray-100 flex items-center justify-center text-sm font-medium text-gray-600">
                        {lead.name.charAt(0)}
                      </div>
                      <div>
                        <p class="font-medium text-charcoal">{lead.name}</p>
                        <p class="text-sm text-gray-500">{lead.email}</p>
                        <p class="text-sm text-gray-400">{lead.company}</p>
                      </div>
                    </div>
                  </td>
                  <td class="py-4 px-6">
                    <span class={`inline-flex items-center px-2.5 py-0.5 rounded-full text-xs font-medium ${statusColors[lead.status] || 'bg-gray-100 text-gray-800'}`}>
                      {statusLabels[lead.status] || lead.status}
                    </span>
                  </td>
                  <td class="py-4 px-6">
                    <p class="text-sm text-gray-600 max-w-xs truncate">{lead.message}</p>
                  </td>
                  <td class="py-4 px-6">
                    <span class="text-sm text-gray-500">{new Date(lead.createdAt).toLocaleDateString()}</span>
                  </td>
                  <td class="py-4 px-6 text-right">
                    <div class="flex items-center justify-end gap-2">
                      <a
                        href={`/admin/conversations?id=${lead.id}`}
                        class="text-sm text-pounce-orange hover:text-pounce-orange-dark font-medium"
                      >
                        View
                      </a>
                    </div>
                  </td>
                </tr>
              ))}
            </tbody>
          </table>
        </div>
      </div>
    </div>
  </AdminLayout>
</AdminPage>
